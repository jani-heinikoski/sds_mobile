<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:id="@+id/dialog_item_container" xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp">

    <TextView
        android:id="@+id/dialog_item_game_over_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:text="Sample Text"
        android:textColor="?attr/colorOnBackground"
        android:textSize="24sp" />

    <Button
        android:id="@+id/dialog_item_button_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/dialog_item_game_over_text"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="back to menu"
        android:textColor="?attr/colorOnSurface"
        android:textSize="20sp" />


</RelativeLayout>